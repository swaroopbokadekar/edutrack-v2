spring.application.name=edutrack-app

# Database Connection (Ensure port is 3306 for standard MySQL)
spring.datasource.url=jdbc:mysql://localhost:3305/edutrack_db?useSSL=false&allowPublicKeyRetrieval=true
spring.datasource.username=root
spring.datasource.password=@swaroop2004

# Hibernate Strategy
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true